{
	animationsConfig: config/animation/npc/griffin.json,
	stateConfig: config/npc/states/griffin.json,
	initialState: idle,
	
	bodyWidth: 1f,
	bodyHeight: 1f,
	useSensorForBody: true, // must not collide with other objects (because it's flying)
	
	imageOffsetX: 0f,
	imageOffsetY: 0.2f,
	
	interactionMarkerOffsetX: 0f,
	interactionMarkerOffsetY: 0f,
}